{
  "name": "sugarcrest-booking",
  "version": "1.0.0",
  "description": "SugarCrest vacation rental booking system — Sugar Lake, MN",
  "private": true,
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'Static site — no build step required'",
    "deploy": "netlify deploy --prod"
  },
  "dependencies": {
    "@netlify/blobs": "^8.0.0",
    "nodemailer": "^6.9.13",
    "stripe": "^16.0.0",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": ["vacation-rental", "stripe", "netlify", "sugarcrest"],
  "author": "SugarCrest LLC"
}
